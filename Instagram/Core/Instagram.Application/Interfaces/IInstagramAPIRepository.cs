using SNM.Instagram.Application.DTO;


namespace SNM.Instagram.Application.Interfaces
{
    public interface IInstagramAPIRepository<Guid>
    {
        //Task<Tuple<int, string>> PublishPost(InstagramChannelPostDto instagramChannelPostDto);
        //Task<Tuple<int, string>> PublishSimplePost(InstagramChannelDto instagramChannelDto, string postText);
        //Task<string> PublishToInstagram(InstagramChannelDto instagramChannelDto, string postText, string pictureURL);
        //Task<Tuple<int, string>> UploadPhoto(InstagramChannelDto instagramChannelDto, string photoURL);
        //Task<Tuple<int, string>> UpdatePost(InstagramChannelPostDto instagramChannelPostDto, string postId);
        
    }
}
